# ── Dependencies ─────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Secrets / env ────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── Database (local) ─────────────────────────
data.db
data.db-shm
data.db-wal
*.sqlite
*.sqlite3

# ── RECAP 2 : JSON déposés (noms de clients) ──
# En production ils vivent dans le volume ($DB_DIR/recap2/). En local, DB_DIR
# vaut la racine du projet : ce dossier peut donc apparaître ici. Jamais versionné.
recap2/

# ── Runtime / logs ───────────────────────────
*.log
logs/
pids/
*.pid
*.seed

# ── OS / editor ──────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# ── Claude Code workspace ────────────────────
.claude/

# ── Legacy / backup files ────────────────────
production_*
*.bak
*.backup

# ── Design system kit (référence locale, pas dans le build) ──
design-system/
