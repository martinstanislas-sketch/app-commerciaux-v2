{
  "name": "crm-automation",
  "version": "0.1.0",
  "private": true,
  "description": "Automatisation CRM (Playwright) — isolée de l'app principale, jamais déployée",
  "scripts": {
    "open": "node open-crm.js"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
